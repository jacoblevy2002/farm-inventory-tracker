<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/tool_container"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <TextView
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="@dimen/recycler_margin_padding"
        android:textSize="@dimen/recycler_text_size"
        android:textColor="@color/black"
        android:id="@+id/tool_stock"
        android:text="No Tool"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        app:layout_constraintTop_toBottomOf="@id/tool_stock"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="@dimen/recycler_margin_padding"
        android:textSize="@dimen/recycler_text_size"
        android:textColor="@color/black"
        android:id="@+id/tool_price"
        android:textStyle="italic"
        android:text="No Tool"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="@dimen/recycler_margin_padding"
        android:textSize="@dimen/recycler_text_size"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:id="@+id/type"
        android:text="No Tool"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>